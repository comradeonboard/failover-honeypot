services:
  app:
    build: .
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "3000:8000"
    command: ["python", "-m", "uvicorn", "backend:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    restart: unless-stopped
